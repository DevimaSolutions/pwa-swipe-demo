const styles = {
  container: {
    height: 'calc(100% - 16rem)',
    width: 'calc(100% - 4rem)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
} as const;

export default styles;
